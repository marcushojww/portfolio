import { Flex, Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const SkillsSection = styled(Flex)``

export const SkillsType = styled(Box)`
  width: 130px;
  font-weight: bold;
`

export const SkillsValues = styled(Box)`
  flex-grow: 1;
`
